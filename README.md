# GPT2_Text_Generation
GPT-2 based text generation project using Hugging Face Transformers and PyTorch. Fine-tunes a pre-trained GPT-2 model on a custom dataset to generate coherent and contextually relevant text from user prompts.
